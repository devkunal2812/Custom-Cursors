import styles from './Footer.module.css';

export default function Footer() {
  return (
    <footer className={styles.footer}>
      Built with ♥ by <a href="#">Kunal's Build</a> - Free forever. No attribution required.
    </footer>
  );
}
